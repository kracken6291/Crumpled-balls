class paper{
    constructor(x,y,r){
        this.x=x;
        this.y=y;
        this.r=r;
        this.width=width;
        this.height=this.height;
        
        var options={
            isStatic: false,
            restituition:0.3,
            friction:0,
            density:1.2
        }
    }
}